// strictly local to the file(private)
const secret = "SUPER SECRET DATA"

// shared with other files that import them(like - protected)
const sathish = 'sathish'
const dhivya = 'dhivya'

module.exports = {sathish, dhivya}